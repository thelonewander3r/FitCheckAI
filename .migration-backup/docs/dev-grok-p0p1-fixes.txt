You are a senior TypeScript developer applying fixes from a code review for this Next.js 16 repo (InterviewReadyAI). Implement EXACTLY these fixes. Do NOT run npm, node, or build commands (WSL lacks the Windows node_modules). Only edit files. Keep changes minimal.

=== FIX 1 (HIGH) — src/app/interview/page.tsx — inverted skin-tone classifier ===
The function that maps average RGB to a skinTone bucket uses HSL lightness thresholds the wrong way: fair skin has HIGH lightness (~80-88) and deep skin has LOW lightness (~20-35), but the code maps <45 to "fair" and high values to "deep". Reverse the thresholds so: lightness <25 -> "deep", <35 -> "tan", <50 -> "medium", <60 -> "light", else (>=60) -> "fair". The inconclusive guards (very low <18 or very high >92) should return null. Keep the same function name and the prefill/hint behavior.

=== FIX 2 (MEDIUM) — src/lib/interview/context-engine.ts — inert positive cultureFormality ===
The dress-code level is an integer in {0,1,2} with thresholds >=2 and >=1, but corporate/government/client-facing add +0.5/+0.25 which never crosses a threshold. Fix: after applying the culture delta, round to the nearest integer (Math.round) before clamping to [0,2] and choosing the label. Also update the culture deltas to: corporate +1, government +1, client-facing +0.5, startup -1, creative -1. (With Math.round, +0.5 now crosses thresholds in both directions.)

=== FIX 3 (MEDIUM) — src/app/interview/[sessionId]/try-on/page.tsx — flatteringColors never reaches composer ===
Currently: palette: session.context.recommendedColors ?? session.context.flatteringColors — recommendedColors is always populated so flatteringColors is dead. Change to merge both, deduped and lowercased: const palette = Array.from(new Set([...(session.context.flatteringColors ?? []), ...(session.context.recommendedColors ?? [])].map(c => c.toLowerCase()))). Pass that as palette.

=== FIX 4 (MEDIUM) — src/app/interview/page.tsx and try-on/page.tsx — presentation default ===
- In the intake form: EMPTY_FORM.presentation should be "" (empty string) and the Select should have an option { value: "", label: "Prefer not to say" } plus feminine/masculine/neutral. Only include presentation in the submit payload when it is non-empty.
- In try-on/page.tsx: change the composer context fallback from presentation: session.intake.presentation ?? "feminine" to ?? "neutral".

=== FIX 5 (MEDIUM) — src/app/interview/page.tsx — skin-tone sampling must not discard a valid upload ===
The downscale try block currently includes await sampleSkinToneFromBase64(base64) and any throw there resets imageBase64 to undefined with an error. Move the skin-tone sampling AFTER setImageBase64(base64) succeeds, wrapped in its own try/catch that silently ignores failures (never clears the image, never shows an error). The hint text should only appear when a tone was actually detected.

=== FIX 6 (LOW) — src/lib/outfits/ranking.ts — clamp overall ===
In computeOverall (or wherever the total is assembled), clamp the final score: Math.min(100, Math.max(0, total)) before assigning overall, so future templates cannot push a displayed score over 100.

=== FIX 7 (LOW) — src/lib/wardrobe/composer.ts — accent penalty vs palette conflict ===
The accent-color penalty should exclude colors that are in the context palette: when counting non-neutral colors for the penalty, skip colors that appear in ctx.palette (lowercased comparison). Keep the +3 palette bonus as is.

=== FIX 8 (LOW) — composer gaps + try-on rendering ===
- In buildGaps: only report gaps for categories the composition actually needs: tops, bottoms, dresses, outerwear, shoes. Drop accessories from gap reporting. Only report a "dresses" gap when there is no valid top+bottom path available (i.e. the wardrobe lacks both a top and a bottom at the required level).
- In try-on/page.tsx wardrobe section: destructure gaps from composeOutfits and render them as a small muted list under the combos when non-empty (prefix "Missing pieces:"), or hide when empty.

=== FIX 9 (LOW) — wardrobe API hardening ===
- src/app/api/wardrobe/route.ts and [id]/route.ts: imageBase64 must match the base64 alphabet check /^[A-Za-z0-9+/]+={0,2}$/ (reject with 422 otherwise), in addition to the existing min/max length.
- seasons: add .min(1) so an empty array is rejected.
- [id]/route.ts PATCH: add a refine that at least one key besides nothing is provided (reject PATCH {} with 422).

=== FIX 10 (LOW) — docs/privacy-and-safety.md ===
Add a paragraph covering the wardrobe store: piece photos are downscaled on the client, stored as base64 in .data/wardrobe.json with no automatic TTL, accessible on localhost without authentication, and should be deleted with the data file. Keep the existing sessions.json paragraph.

=== TESTS ===
- src/lib/interview/context-engine.test.ts: add a positive-direction culture test (companyCulture "corporate" raises the formality label vs none).
- src/lib/wardrobe/composer.test.ts: strengthen the outerwear-rescue test so it asserts a top one level below IS rescued by a qualifying jacket (not just that a gap exists); add a test that a complete wardrobe reports no spurious gaps.
- src/lib/outfits/ranking.test.ts: add a test that overall never exceeds 100 even with all bonuses maxed.

When done, list every file changed with a one-line summary. Do not touch anything else.
