You are a senior TypeScript developer implementing Phase 3 (outfit history & learning) for this Next.js 16 repo (InterviewReadyAI). The app already composes outfits from a user wardrobe for occasions. P3 adds: marking outfits as worn, storing worn history, deriving a style profile (preferred colors/categories/formality), and feeding that profile back into the composer so recommendations learn. Implement EXACTLY this spec. Do NOT run npm, node, or build commands (WSL lacks the Windows node_modules). Only edit files. Match existing style (see wardrobe-store, occasion-store, venue/types, composer.ts, occasion pages).

=== 1. src/types/worn.ts (new) ===
import type { WardrobeCategory, WardrobeColor, WardrobeFormality } from "./wardrobe"
import type { OccasionType } from "./occasion"
export interface WornItemRef { id: string; name: string; category: WardrobeCategory; color: WardrobeColor; formality: WardrobeFormality }
export interface WornOutfitRecord {
  id: string
  occasionId?: string
  eventType?: OccasionType | "general"
  wornDate: string       // ISO date (YYYY-MM-DD)
  rating?: "loved" | "liked" | "meh"
  items: WornItemRef[]
  createdAt: string
}
export interface StyleProfile {
  colors: { color: WardrobeColor; count: number }[]      // top 3 by count
  categories: { category: WardrobeCategory; count: number }[] // top 2 by count
  formality: number     // rounded mean of item formality levels (0..4), or -1 when no data
  totalWorn: number
}

=== 2. src/lib/wardrobe/formality.ts (new) ===
Export formalityToLevel(label: WardrobeFormality): number — casual 0, smart-casual 1, business-casual 2, business-professional 3, formal 4. Export formalityLevelToLabel(level: number): WardrobeFormality with Number.isFinite guard and clamp to [0,4] (return "smart-casual" for non-finite). NOTE: src/lib/venue/types.ts currently defines a DressCodeLabel + formalityLevelToLabel — refactor so src/lib/wardrobe/formality.ts is the single source of truth: have src/lib/venue/types.ts re-export from it (type DressCodeLabel = WardrobeFormality stays, and formalityLevelToLabel re-exported) so the venue module keeps working without duplication. Check existing imports (src/lib/services/occasion-service.ts, venue/index.ts, mock-provider.ts) still resolve.

=== 3. src/lib/worn-store.ts (new) ===
Mirror wardrobe-store/occasion-store exactly: .data/worn.json (array shape), withLock, atomic tmp+rename, ENOENT -> []. Export addRecord(record: Omit<WornOutfitRecord, "id" | "createdAt">): Promise<WornOutfitRecord> (id = crypto.randomUUID(), createdAt now), listRecords(): Promise<WornOutfitRecord[]> (sorted by wornDate desc, then createdAt desc), deleteRecord(id): Promise<boolean>.

=== 4. src/lib/services/style-service.ts (new) ===
- export function buildStyleProfile(records: WornOutfitRecord[]): StyleProfile — aggregate across all records: count colors of items, count categories of items, formality = Math.round(mean of formalityToLevel(item.formality)) or -1 when no items, totalWorn = records.length, colors = top 3 by count (ties broken by first-seen), categories = top 2 by count. Empty records -> empty arrays, formality -1, totalWorn 0.
- export function preferencesFromProfile(profile: StyleProfile): { colors?: string[]; categories?: WardrobeCategory[]; formality?: number } — colors = top colors (up to 3, only when count >= 2 to avoid one-off noise), categories = top categories (up to 2, count >= 2), formality = profile.formality when profile.formality >= 0. Empty profile -> {}.

=== 5. src/lib/wardrobe/composer.ts — preferences hook ===
- ComposeContext gains optional preferences?: { colors?: string[]; categories?: WardrobeCategory[]; formality?: number }.
- In scoring, add bounded preference bonuses per item: +2 if item.color (lowercased) is in preferences.colors (cap +4 total), +1 if item.category is in preferences.categories (cap +2 total), +1 if formalityToLevel(item.formality) === preferences.formality (cap +2 total). All additive and bounded; existing scores unchanged when preferences is absent.

=== 6. src/lib/services/occasion-service.ts — feed preferences into composition ===
In createOccasion, after loading wardrobe items and before composing: load worn records (listRecords), build profile = buildStyleProfile(records), preferences = preferencesFromProfile(profile). Pass preferences into composeOutfits context. Keep everything else identical.

=== 7. API ===
a) src/app/api/worn/route.ts (new): POST — parse JSON (400 invalid), zod validate: occasionId optional string max 100, eventType optional enum of OCCASION_TYPES + "general", wornDate optional string max 32 (default today's YYYY-MM-DD), rating optional enum ["loved","liked","meh"], items required array of objects { id: string max 100, name: string max 200, category: enum WARDROBE_CATEGORIES, color: enum WARDROBE_COLORS, formality: enum WARDROBE_FORMALITY } with min 1 max 12 items. 422 on failure with { error, issues }. addRecord -> 201 { id }. GET: return { records, profile } where records = listRecords() and profile = buildStyleProfile(records).
b) src/app/api/worn/[id]/route.ts (new): DELETE — UUID validate (400), deleteRecord (404 if false), 204.

=== 8. UI ===
a) src/app/occasion/[id]/page.tsx — on each outfit card (best + alternatives), add a "Mark as worn" button. On click: POST /api/worn with { occasionId: session.id, eventType: session.intake.eventType, items: outfit.items.map(({ id, name, category, color, formality }) => ({ id, name, category, color, formality })) }. On success, replace the button with a small "Worn ✓" label for that outfit (local state map outfitId -> marked). On failure, show a muted inline error.
b) src/app/wardrobe/page.tsx — add two sections below the grid:
   - "Recently worn": fetch /api/worn on mount, show the last 5 records (date, eventType, item names joined, rating badge if present). Empty state: "No worn outfits yet — mark outfits as worn from an occasion page."
   - "Your style profile": from the same response's profile: show top colors, top categories, typical formality label (formalityLevelToLabel), and "N outfits worn" count. Empty state when totalWorn is 0: "Wear a few outfits and we'll learn your style."

=== 9. Tests ===
- src/lib/services/style-service.test.ts (new): aggregation (two records -> top colors/categories correct, formality mean rounded), empty records -> empty profile, preferencesFromProfile thresholds (count >= 2 gating; single one-off color not included).
- src/lib/wardrobe/composer.test.ts: add one test that a preference for a color/category raises that combo's score vs the same setup without preferences (keep all existing tests passing).

When done, list every file changed (new or modified) with a one-line summary. Do not touch anything else.
