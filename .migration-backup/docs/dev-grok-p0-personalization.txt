You are a senior TypeScript developer implementing Phase 0 (person profile personalization) for this Next.js 16 repo (InterviewReadyAI). The product becomes a personal stylist: recommendations must adjust to the person (fit size, weight, skin tone, presentation, company culture). Implement EXACTLY this spec. Do NOT run npm, node, or build commands (WSL lacks the Windows node_modules). Only edit files. Preserve existing behavior and tests; add tests only where the spec says. Keep changes minimal and consistent with existing code style.

=== 1. src/lib/validation/schemas.ts ===
Add these OPTIONAL fields to IntakeSchema (all optional so existing flows keep working):
- fitSize: z.string().max(20).optional()   (free text like "US 6" or "M")
- weightLbs: z.number().positive().max(1000).optional()
- skinTone: z.enum(["fair", "light", "medium", "tan", "deep"]).optional()
- presentation: z.enum(["feminine", "masculine", "neutral"]).optional()
- companyCulture: z.enum(["corporate", "startup", "creative", "client-facing", "government"]).optional()

=== 2. src/lib/interview/context-engine.ts ===
The InterviewContext type (src/types/interview.ts) gains two optional fields; add them to the type too:
- flatteringColors?: string[]  (derived from skin tone)
- cultureFormality?: number    (-1 to +1 adjustment)

In inferInterviewContext:
a) Skin tone mapping (flatteringColors), using this exact map when input.skinTone is present:
   fair:   ["navy", "black", "white", "burgundy"]
   light:  ["navy", "charcoal", "sage", "cream"]
   medium: ["navy", "olive", "rust", "cream"]
   tan:    ["black", "white", "gold", "deep green"]
   deep:   ["white", "emerald", "gold", "royal blue"]
   When absent, do not set flatteringColors (existing palette logic unchanged).
b) Company culture adjustment (cultureFormality), from input.companyCulture:
   corporate: +0.5, startup: -0.5, creative: -0.5, client-facing: +0.25, government: +0.5.
   Apply it to the resolved formality level: add a comment that the numeric
   formality index is adjusted by cultureFormality before the dress-code label
   is chosen, and make the dress-code label resolution actually use it
   (i.e. clamp the adjusted index and map to the same label set). When
   companyCulture is absent, no adjustment.
c) The intake type (IntakePayload in src/types/interview.ts) must include the
   new optional fields so they flow through.

=== 3. src/lib/outfits/templates.ts and src/lib/outfits/ranking.ts ===
a) templates.ts: add an optional presentation field to each OutfitTemplate:
   "feminine" | "masculine" | "neutral". Reasonable defaults per existing
   garment notes (the items already mentioning feminine silhouettes become
   "feminine"; items with gender-adaptable notes become "neutral"; any
   masculine-leaning items "masculine"). Do not change names or ids.
b) ranking.ts: add two small scoring bonuses in the existing score functions:
   - presentation bonus: when the user presentation is provided and matches a
     template presentation, +3; neutral templates always get +1 (safe default).
   - color bonus: when the context has flatteringColors and the outfit's
     palette (outfit.colors array, lowercase) intersects them, +2 per
     intersecting color (capped at +4 total).
   Keep every existing score dimension unchanged; these bonuses are additive
   at the total. Keep the same return shape.
c) selectTopOutfits signature: add an optional person profile parameter
   ({ fitSize?, weightLbs?, skinTone?, presentation? }) and pass it into the
   scoring. Update session-service call site accordingly (see 4).
   Do NOT use fitSize/weightLbs in numeric scoring yet — instead attach a
   fitNote string to each RankedOutfit when fitSize or weightLbs is present,
   e.g. "Tailoring note: confirm jacket shoulders for US 6." (use the actual
   fitSize value). Keep it a single short string.

=== 4. src/lib/services/session-service.ts ===
In analyzeSession, pass the person profile (from intake: fitSize, weightLbs,
skinTone, presentation, companyCulture) into inferInterviewContext and
selectTopOutfits. The context engine and ranking already read them.

=== 5. src/app/interview/page.tsx ===
Add form fields (all optional, styled like existing inputs/selects):
- Fit size (optional) — Input, placeholder "e.g. US 6 or M"
- Weight (lbs, optional) — Input type number
- Skin tone (optional) — Select: fair, light, medium, tan, deep
- Presentation — Select: feminine (default), masculine, neutral
- Company culture (optional) — Select: corporate, startup, creative,
  client-facing, government, with an option label "Auto (from industry)"
  mapped to undefined.
Add these to FormState, EMPTY_FORM, and the submit payload.

=== 6. Client-side skin tone sampling (src/app/interview/page.tsx) ===
In handleFileChange, after downscaling to canvas and BEFORE toDataURL, sample
the skin tone: draw the same canvas region, get imageData for the center 20%
box, average the RGB, and classify into the skinTone enum using this
heuristic (keep it simple and approximate):
  compute HSL from average RGB; if lightness is very low (<25) or very high
  (>90) treat as inconclusive (do not set skin tone). Otherwise bucket by
  lightness: <45 fair, <55 light, <65 medium, <75 tan, else deep.
When conclusive and the user has not manually chosen a skin tone, prefill the
skinTone select state with the detected value and show a small hint text
"(detected from photo — you can change it)". When inconclusive, leave as is.

=== 7. Tests ===
- src/lib/validation/schemas.test.ts: add cases that the new fields parse and
  optionality holds (absent fields still pass).
- src/lib/interview/context-engine.test.ts: add 2 tests — skinTone "deep"
  yields flatteringColors ["white","emerald","gold","royal blue"]; companyCulture
  "startup" lowers the formality label vs none.
- src/lib/outfits/ranking.test.ts: add 1 test — a matching presentation and
  an intersecting color raise a template's total vs baseline.

When done, list every file changed with a one-line summary. Do not touch
anything else.
