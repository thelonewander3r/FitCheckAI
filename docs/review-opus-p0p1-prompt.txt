You are the lead developer doing a review of two new features before commit: P0 (person-profile personalization) and P1 (wardrobe module) in this Next.js 16 repo (InterviewReadyAI).

Changed/new files to review:
- P0: src/lib/validation/schemas.ts, src/types/interview.ts, src/lib/interview/context-engine.ts, src/lib/outfits/templates.ts, src/lib/outfits/ranking.ts, src/lib/services/session-service.ts, src/app/interview/page.tsx, and their tests.
- P1: src/types/wardrobe.ts, src/lib/wardrobe-store.ts, src/lib/wardrobe/composer.ts, src/lib/wardrobe/composer.test.ts, src/app/api/wardrobe/route.ts, src/app/api/wardrobe/[id]/route.ts, src/lib/client/image-utils.ts, src/app/wardrobe/page.tsx, src/app/interview/[sessionId]/try-on/page.tsx.

P0 intent: optional person fields (fitSize, weightLbs, skinTone, presentation, companyCulture) flow from intake through context inference (flatteringColors from skin tone; cultureFormality adjusting the dress-code label) into outfit ranking (presentation bonus, palette-intersection bonus, fitNote text). Client samples a skin-tone bucket from the downscaled selfie and prefills the select.

P1 intent: a file-backed wardrobe (atomic writes, lock, .data/wardrobe.json) with CRUD + favorites API; a My Wardrobe capture UI (image downscale reuse, tags); a pure composition engine that builds outfits from owned pieces gated by formality/season/color/presentation and reports gaps; the try-on page shows a From-your-wardrobe section when combos exist.

Verify specifically:
1. P0 scoring: do the new bonuses stay bounded and cannot make totals go negative or exceed 100? Any interaction with existing scores that could break the demo scenario ranking (outfit-005 should still rank first for the default demo)? Does cultureFormality actually change the label as claimed (clamped properly)?
2. P1 store: is the wardrobe store consistent with the session-store patterns (lock, atomic rename, ENOENT handling, no prototype pollution via ids)?
3. Composer correctness: formality gating (one-step-below rule with outerwear rescue), color harmony penalties, palette bonuses, gap reporting, deterministic ordering. Any infinite loops or explosion risk on large wardrobes?
4. API: validation completeness (empty imageBase64 rejected, size caps), UUID validation, error status codes, favorite PATCH semantics.
5. Privacy/security: imageBase64 stored in .data/wardrobe.json — is it ever leaked in unexpected responses? SVG/data-URL handling. XSS in the wardrobe page (React escaping).
6. Client: downscaleToBase64 refactor did not regress the interview upload (fail-closed behavior, skin-tone sampling still present). Any memory leaks (object URLs).
7. Tests: composer tests meaningful? Any missing critical coverage (store concurrency, PATCH validation)?

All gates are green (typecheck, 85 unit tests, 2 e2e, build). Do NOT modify files. Output: verdicts per area (PASS / PASS-WITH-NOTE / FAIL), any NEW issues with file:line and severity, and a final one-line recommendation (ship / fix first).
