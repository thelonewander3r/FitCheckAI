You are the lead developer doing a review of P2 (occasion/venue intelligence) before commit in this Next.js 16 repo (InterviewReadyAI). The feature: a user plans an occasion (event type, venue/company name, theme), the app runs a venue lookup (mock provider with curated/keyword/event-default matching; live stub behind VENUE_MODE), then composes outfits from the user's wardrobe items with the venue's dress code + palette, returning a venue context card + best outfit + alternatives + gaps.

New/changed files:
- src/types/occasion.ts
- src/lib/venue/types.ts, index.ts, mock-provider.ts, live-provider.ts, mock-provider.test.ts
- src/lib/occasion-store.ts
- src/lib/services/occasion-service.ts
- src/app/api/occasions/route.ts, [id]/route.ts, demo/route.ts
- src/app/occasion/page.tsx, [id]/page.tsx
- src/app/page.tsx, src/app/wardrobe/page.tsx (nav links)

Verify specifically:
1. Mock venue matching: order of curated checks (rooftop before wedding? bank before gala?), substring false positives (e.g. "bank" matching "bankrupt"? acceptable?), formalityLevelToLabel correctness, event-default table sane.
2. Occasion service: error handling when venue lookup throws or wardrobe read fails; palette merge dedupe/case; composer call parameter mapping (formality label from level, presentation default); store write consistency.
3. Occasion store: same lock/atomic/ENOENT patterns; list vs map shape consistent with how API reads it.
4. API: validation completeness (venueName required, lengths), UUID checks, status codes, demo route behavior, no leak of base64 (occasions do not store images, confirm).
5. UI: occasion intake form validation/submit UX; result page rendering when outfits empty vs gaps present; loading/error states; hydration issues in server vs client components.
6. Security/privacy: venueName is user input — any injection into rendered output or into the mock matcher? React escaping. No image data in occasions store.
7. Tests: mock venue tests meaningful; any missing critical coverage (store, service, formalityLevelToLabel boundaries).

All gates are green (typecheck, 93 unit tests, 2 e2e, build). Do NOT modify files. Output: verdicts per area (PASS / PASS-WITH-NOTE / FAIL), NEW issues with file:line and severity, final one-line recommendation.
