You are a senior TypeScript developer implementing personalized virtual try-on for this Next.js 16 repo (InterviewReadyAI). Currently the user's uploaded selfie is used for skin analysis but then DISCARDED — tryOnOutfit hardcodes a 1x1 transparent placeholder PNG, so the VTO preview never shows the person. Implement EXACTLY these changes. Do NOT run npm, node, or build commands (WSL lacks the Windows node_modules). Only edit files. Keep changes minimal. Preserve all existing behavior, types, and tests.

1. src/lib/session-store.ts — add an optional field to the StoredSession interface: userImageBase64?: string. No other store logic changes needed.

2. src/lib/services/session-service.ts —
   a. In analyzeSession (around lines 42-90): after successful analysis, include the image in the final storeUpdate: add userImageBase64: imageBase64 (only when imageBase64 is a non-empty string; otherwise omit the key). Do not store it before analysis completes.
   b. In tryOnOutfit (around lines 92-123): replace the hardcoded PLACEHOLDER_IMAGE_BASE64 with the stored photo: const userImage = session.userImageBase64 && session.userImageBase64.length > 0 ? session.userImageBase64 : PLACEHOLDER_IMAGE_BASE64; then pass userImage as userImageBase64 to runApparelVto.

3. src/lib/youcam/mock-provider.ts — personalize the mock preview:
   a. Add a helper to sniff the image MIME from base64 magic bytes: JPEG starts with /9j/ when base64-encoded (bytes FF D8 FF), PNG starts with iVBOR (89 50 4E 47), WebP starts with UklGR (RIFF....WEBP). Default to image/jpeg.
   b. Change makePlaceholderSvgDataUrl(label) to makePlaceholderSvgDataUrl(label, userImageBase64?) where userImageBase64 is optional. When a non-empty userImageBase64 is provided, embed the photo into the SVG: an image element like <image href="data:image/jpeg;base64,${userImageBase64}" x="100" y="80" width="200" height="440" preserveAspectRatio="xMidYMid slice"/>, then overlay the garment label text (keep the existing label text element) and a small semi-transparent dark strip behind the label so it remains readable. Keep the existing mock placeholder behavior when userImageBase64 is absent (the current rect + label SVG). Keep using Buffer.from(svg, "utf8").toString("base64") for the final data URL.

4. src/app/interview/page.tsx — downscale the uploaded selfie client-side before sending:
   a. In handleFileChange (around lines 49-60): after reading the file, if it is larger than 15 MB, show an inline error message and do not set the image. Otherwise downscale: load the image (HTMLImageElement with URL.createObjectURL or createImageBitmap), scale so the longest edge is at most 512 pixels, draw to a canvas, and export as JPEG quality 0.85 base64 (canvas.toDataURL("image/jpeg", 0.85), strip the data URL prefix). Set the resulting base64 into imageBase64 state.
   b. If downscaling fails for any reason (unsupported format, canvas taint, decode error), fall back to the original file base64 rather than failing the upload.
   c. Keep the existing UI copy.

5. docs/privacy-and-safety.md — reconcile the image-storage claim with reality: state that uploaded selfies are downscaled to max 512px on the client, stored as base64 in the session record (.data/sessions.json) for the duration of the session file's life, not written to separate image files, and that the app currently has no automatic TTL/cleanup sweep. Update the earlier contradicting lines (images not written to disk vs session data lives in sessions.json) so they are consistent.

When done, list every file changed with a one-line summary. Do not touch anything else.
